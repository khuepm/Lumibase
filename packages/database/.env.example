# PostgreSQL connection string used by drizzle-kit (generate/migrate/studio).
# In production, the CMS Worker uses the Hyperdrive binding instead.
DATABASE_URL=postgres://user:password@localhost:5432/lumibase
